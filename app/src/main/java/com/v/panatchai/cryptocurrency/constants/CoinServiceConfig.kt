package com.v.panatchai.cryptocurrency.constants

/**
 * Hold the configuration for Remote Paging.
 */
object CoinServiceConfig {
    /**
     * Size of the remote Page, which means the client will query 20 items at a time.
     */
    const val PAGE_SIZE = 20
}
